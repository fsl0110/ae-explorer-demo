import { FilterValues } from "../../types";
export declare const isFilterActive: (selectedValues: FilterValues, allValues: FilterValues, filter: "text" | "date" | "check" | "slider" | "search" | undefined) => boolean;
