## It's just experimental, and not ready to use!!!

```jsx
const onChange = (payload) => console.log("onChange", payload);
<FilterCheckBlock
  allValues={["Age", "AESER", "AEOUT", "AESEV"]}
  selectedValues={["AESER"]}
  onChange={onChange}
/>;
```
