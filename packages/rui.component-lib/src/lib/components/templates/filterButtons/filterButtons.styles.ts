import styled from "styled-components";
import {Menu} from "antd";

export const FilterButtonStyles = styled.div`
  max-width: 900px;
  margin-bottom: 1em;

  div {
    margin-right: 0.5em;
  }
`;

export const MenuStyles = styled(Menu)`
  padding: 0.5em 1rem !important;
`;
