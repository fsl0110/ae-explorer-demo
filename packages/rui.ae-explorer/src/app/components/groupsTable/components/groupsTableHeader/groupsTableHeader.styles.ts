import styled from "styled-components";

export const HeaderStyles = styled.thead`
  background-color: #fafafa;

  th {
    padding: 1em 1em 0;
    font-weight: 500;
    text-align: center;
  }

  .subvalue {
    font-size: 13px;
    font-weight: 400;
    color: inherit;
  }
`;
